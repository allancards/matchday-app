import { defineConfig } from 'prisma/config';

export default defineConfig({
  schema: 'prisma/schema.prisma',
  // 💡 Adicione o bloco abaixo para o Prisma Migrate funcionar:
  datasource: {
    url: process.env.DATABASE_URL || 'file:./dev.db',
  },
});
